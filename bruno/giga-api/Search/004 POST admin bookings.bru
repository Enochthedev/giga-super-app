meta {
  name: POST admin bookings
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/search/admin/bookings
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/search/admin/bookings`

Service: **search-service** · file: `search-service/src/routes/admin.ts` · route: `/bookings`.

Auth: bearer ({{token}}).
}
