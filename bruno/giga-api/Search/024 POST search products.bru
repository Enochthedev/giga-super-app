meta {
  name: POST search products
  type: http
  seq: 24
}

post {
  url: {{baseUrl}}/api/v1/search/products
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/search/products`

Service: **search-service** · file: `search-service/src/routes/products.ts` · route: `/`.

Auth: bearer ({{token}}).
}
