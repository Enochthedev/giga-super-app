meta {
  name: POST admin orders
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/search/admin/orders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/search/admin/orders`

Service: **search-service** · file: `search-service/src/routes/admin.ts` · route: `/orders`.

Auth: bearer ({{token}}).
}
