meta {
  name: POST search drivers
  type: http
  seq: 15
}

post {
  url: {{baseUrl}}/api/v1/search/drivers
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/search/drivers`

Service: **search-service** · file: `search-service/src/routes/drivers.ts` · route: `/`.

Auth: bearer ({{token}}).
}
