meta {
  name: GET search stats
  type: http
  seq: 36
}

get {
  url: {{baseUrl}}/api/v1/search/stats
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/search/stats`

Service: **search-service** · file: `search-service/src/routes/search.ts` · route: `/stats`.

Auth: bearer ({{token}}).
}
