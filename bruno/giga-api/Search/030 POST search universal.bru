meta {
  name: POST search universal
  type: http
  seq: 30
}

post {
  url: {{baseUrl}}/api/v1/search/universal
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/search/universal`

Service: **search-service** · file: `search-service/src/routes/search.ts` · route: `/universal`.

Auth: bearer ({{token}}).
}
