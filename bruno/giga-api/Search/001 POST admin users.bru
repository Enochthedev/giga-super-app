meta {
  name: POST admin users
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/search/admin/users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/search/admin/users`

Service: **search-service** · file: `search-service/src/routes/admin.ts` · route: `/users`.

Auth: bearer ({{token}}).
}
