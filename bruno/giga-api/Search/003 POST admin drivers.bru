meta {
  name: POST admin drivers
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/search/admin/drivers
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/search/admin/drivers`

Service: **search-service** · file: `search-service/src/routes/admin.ts` · route: `/drivers`.

Auth: bearer ({{token}}).
}
