meta {
  name: GET admin rides
  type: http
  seq: 14
}

get {
  url: {{baseUrl}}/api/v1/search/admin/rides
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/search/admin/rides`

Service: **search-service** · file: `search-service/src/routes/admin.ts` · route: `/rides`.

Auth: bearer ({{token}}).
}
