meta {
  name: POST admin vendors
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/search/admin/vendors
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/search/admin/vendors`

Service: **search-service** · file: `search-service/src/routes/admin.ts` · route: `/vendors`.

Auth: bearer ({{token}}).
}
