meta {
  name: POST admin transactions
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/search/admin/transactions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/search/admin/transactions`

Service: **search-service** · file: `search-service/src/routes/admin.ts` · route: `/transactions`.

Auth: bearer ({{token}}).
}
