meta {
  name: POST search hotels
  type: http
  seq: 20
}

post {
  url: {{baseUrl}}/api/v1/search/hotels
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/search/hotels`

Service: **search-service** · file: `search-service/src/routes/hotels.ts` · route: `/`.

Auth: bearer ({{token}}).
}
