meta {
  name: DELETE search cache
  type: http
  seq: 35
}

delete {
  url: {{baseUrl}}/api/v1/search/cache
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`DELETE /api/v1/search/cache`

Service: **search-service** · file: `search-service/src/routes/search.ts` · route: `/cache`.

Auth: bearer ({{token}}).
}
