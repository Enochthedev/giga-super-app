meta {
  name: POST search analytics
  type: http
  seq: 33
}

post {
  url: {{baseUrl}}/api/v1/search/analytics
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/search/analytics`

Service: **search-service** · file: `search-service/src/routes/search.ts` · route: `/analytics`.

Auth: bearer ({{token}}).
}
