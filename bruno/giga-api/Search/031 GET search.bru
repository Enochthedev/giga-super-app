meta {
  name: GET search
  type: http
  seq: 31
}

get {
  url: {{baseUrl}}/api/v1/search
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/search`

Service: **search-service** · file: `search-service/src/routes/search.ts` · route: `/`.

Auth: bearer ({{token}}).
}
