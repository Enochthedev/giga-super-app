meta {
  name: Switch Role
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/roles/switch
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "role": "customer"
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`switch-role`.
}
