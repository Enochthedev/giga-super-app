meta {
  name: DELETE connections
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/api/v1/connections/:connectionId
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`DELETE /api/v1/connections/:connectionId`

Service: **social-service** · file: `social-service/src/routes/connections.ts` · route: `/:connectionId`.

Auth: bearer ({{token}}).
}
