meta {
  name: POST posts report
  type: http
  seq: 21
}

post {
  url: {{baseUrl}}/api/v1/posts/{{postId}}/report
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/posts/{{postId}}/report`

Service: **social-service** · file: `social-service/src/routes/posts.ts` · route: `/{{postId}}/report`.

Auth: bearer ({{token}}).
}
