meta {
  name: GET stories
  type: http
  seq: 24
}

get {
  url: {{baseUrl}}/api/v1/stories/:storyId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/stories/:storyId`

Service: **social-service** · file: `social-service/src/routes/stories.ts` · route: `/:storyId`.

Auth: bearer ({{token}}).
}
