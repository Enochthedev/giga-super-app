meta {
  name: GET connections
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/connections
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/connections`

Service: **social-service** · file: `social-service/src/routes/connections.ts` · route: `/`.

Auth: bearer ({{token}}).
}
