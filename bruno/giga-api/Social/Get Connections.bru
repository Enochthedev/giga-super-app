meta {
  name: Get Connections
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/api/v1/connections
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
Lists your connections. (Fixed E6 — gateway now routes /api/v1/connection*.)
}
