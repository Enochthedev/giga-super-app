meta {
  name: POST posts
  type: http
  seq: 18
}

post {
  url: {{baseUrl}}/api/v1/posts
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "content": "Hello from Bruno",
    "visibility": "public"
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/posts`

Service: **social-service** · file: `social-service/src/routes/posts.ts` · route: `/`.

Auth: bearer ({{token}}).
}
