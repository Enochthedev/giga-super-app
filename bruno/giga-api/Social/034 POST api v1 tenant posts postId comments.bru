meta {
  name: POST api v1 tenant posts postId comments
  type: http
  seq: 34
}

post {
  url: {{baseUrl}}/api/v1/tenant/posts/:postId/comments
  body: json
  auth: bearer
}

params:path {
  postId: 8e067652-959e-482a-8979-e6cf8a681b1b
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "_replace": "real fields required - never send an empty {} body (defect V3)"
  }
}

tests {
  test("reached backend (verified 400 on 2026-08-18)", function(){
    expect(res.status).to.be.below(500);
  });
}

docs {
  `POST /api/v1/tenant/posts/:postId/comments`
  
  **✅ reachable (probe payload → 400)** — last verified 2026-08-18, HTTP `400`.
  
  Service: **social-service** · file: `social-service/src/routes/tenant-posts.ts`
  Auth: customer bearer (`{{token}}`).
}
