meta {
  name: PUT posts
  type: http
  seq: 19
}

put {
  url: {{baseUrl}}/api/v1/posts/{{postId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`PUT /api/v1/posts/{{postId}}`

Service: **social-service** · file: `social-service/src/routes/posts.ts` · route: `/{{postId}}`.

Auth: bearer ({{token}}).
}
