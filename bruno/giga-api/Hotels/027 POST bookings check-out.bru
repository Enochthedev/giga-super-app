meta {
  name: POST bookings check-out
  type: http
  seq: 27
}

post {
  url: {{baseUrl}}/api/v1/management/bookings/:id/check-out
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/management/bookings/:id/check-out`

Service: **hotels-service** · file: `hotels-service/src/routes/management.ts` · route: `/bookings/:id/check-out`.

Auth: bearer ({{token}}).
}
