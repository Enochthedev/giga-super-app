meta {
  name: GET reviews my-reviews
  type: http
  seq: 36
}

get {
  url: {{baseUrl}}/api/v1/reviews/my-reviews
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/reviews/my-reviews`

Service: **hotels-service** · file: `hotels-service/src/routes/reviews.ts` · route: `/my-reviews`.

Auth: bearer ({{token}}).
}
