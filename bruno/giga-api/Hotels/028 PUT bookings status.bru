meta {
  name: PUT bookings status
  type: http
  seq: 28
}

put {
  url: {{baseUrl}}/api/v1/management/bookings/:id/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "status": "active"
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`PUT /api/v1/management/bookings/:id/status`

Service: **hotels-service** · file: `hotels-service/src/routes/management.ts` · route: `/bookings/:id/status`.

Auth: bearer ({{token}}).
}
