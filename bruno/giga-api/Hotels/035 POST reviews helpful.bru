meta {
  name: POST reviews helpful
  type: http
  seq: 35
}

post {
  url: {{baseUrl}}/api/v1/reviews/:id/helpful
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/reviews/:id/helpful`

Service: **hotels-service** · file: `hotels-service/src/routes/reviews.ts` · route: `/:id/helpful`.

Auth: bearer ({{token}}).
}
