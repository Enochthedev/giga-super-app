meta {
  name: POST bookings check-in
  type: http
  seq: 26
}

post {
  url: {{baseUrl}}/api/v1/management/bookings/:id/check-in
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/management/bookings/:id/check-in`

Service: **hotels-service** · file: `hotels-service/src/routes/management.ts` · route: `/bookings/:id/check-in`.

Auth: bearer ({{token}}).
}
