meta {
  name: GET bookings
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/bookings
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/bookings`

Service: **hotels-service** · file: `hotels-service/src/routes/bookings.ts` · route: `/`.

Auth: bearer ({{token}}).
}
