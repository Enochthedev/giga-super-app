meta {
  name: GET favorites
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/favorites
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/favorites`

Service: **hotels-service** · file: `hotels-service/src/routes/favorites.ts` · route: `/`.

Auth: bearer ({{token}}).
}
