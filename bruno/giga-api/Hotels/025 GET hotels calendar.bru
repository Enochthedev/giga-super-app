meta {
  name: GET hotels calendar
  type: http
  seq: 25
}

get {
  url: {{baseUrl}}/api/v1/management/hotels/:hotelId/calendar
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/management/hotels/:hotelId/calendar`

Service: **hotels-service** · file: `hotels-service/src/routes/management.ts` · route: `/hotels/:hotelId/calendar`.

Auth: bearer ({{token}}).
}
