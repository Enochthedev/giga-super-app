meta {
  name: GET hotels reviews
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/v1/hotels/:id/reviews
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/hotels/:id/reviews`

Service: **hotels-service** · file: `hotels-service/src/routes/hotels.ts` · route: `/:id/reviews`.

Auth: bearer ({{token}}).
}
