meta {
  name: POST favorites add
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/v1/favorites/add
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "hotel_id": "{{hotelId}}"
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/favorites/add`

Service: **hotels-service** · file: `hotels-service/src/routes/favorites.ts` · route: `/add`.

Auth: bearer ({{token}}).
}
