meta {
  name: GET hotels recommended
  type: http
  seq: 14
}

get {
  url: {{baseUrl}}/api/v1/hotels/recommended
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/hotels/recommended`

Service: **hotels-service** · file: `hotels-service/src/routes/hotels.ts` · route: `/recommended`.

Auth: bearer ({{token}}).
}
