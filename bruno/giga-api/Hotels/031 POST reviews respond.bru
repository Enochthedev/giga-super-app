meta {
  name: POST reviews respond
  type: http
  seq: 31
}

post {
  url: {{baseUrl}}/api/v1/management/reviews/:id/respond
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/management/reviews/:id/respond`

Service: **hotels-service** · file: `hotels-service/src/routes/management.ts` · route: `/reviews/:id/respond`.

Auth: bearer ({{token}}).
}
