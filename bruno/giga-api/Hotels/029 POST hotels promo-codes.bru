meta {
  name: POST hotels promo-codes
  type: http
  seq: 29
}

post {
  url: {{baseUrl}}/api/v1/management/hotels/:hotelId/promo-codes
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/management/hotels/:hotelId/promo-codes`

Service: **hotels-service** · file: `hotels-service/src/routes/management.ts` · route: `/hotels/:hotelId/promo-codes`.

Auth: bearer ({{token}}).
}
