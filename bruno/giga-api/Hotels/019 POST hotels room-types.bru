meta {
  name: POST hotels room-types
  type: http
  seq: 19
}

post {
  url: {{baseUrl}}/api/v1/management/hotels/:hotelId/room-types
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/management/hotels/:hotelId/room-types`

Service: **hotels-service** · file: `hotels-service/src/routes/management.ts` · route: `/hotels/:hotelId/room-types`.

Auth: bearer ({{token}}).
}
