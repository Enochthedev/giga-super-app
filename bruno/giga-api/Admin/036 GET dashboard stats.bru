meta {
  name: GET dashboard stats
  type: http
  seq: 36
}

get {
  url: {{baseUrl}}/api/dashboard/stats
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/dashboard/stats`

Service: **admin-service** · file: `admin-service/src/routes/dashboard.ts` · route: `/stats`.

Auth: admin bearer ({{adminToken}}).
}
