meta {
  name: GET hotel hotels
  type: http
  seq: 59
}

get {
  url: {{baseUrl}}/api/managers/hotel/hotels
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/managers/hotel/hotels`

Service: **admin-service** · file: `admin-service/src/routes/modules/hotel.ts` · route: `/hotels`.

Auth: admin bearer ({{adminToken}}).
}
