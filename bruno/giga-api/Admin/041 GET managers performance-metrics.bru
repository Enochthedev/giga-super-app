meta {
  name: GET managers performance-metrics
  type: http
  seq: 41
}

get {
  url: {{baseUrl}}/api/managers/performance-metrics
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/managers/performance-metrics`

Service: **admin-service** · file: `admin-service/src/routes/managers.ts` · route: `/performance-metrics`.

Auth: admin bearer ({{adminToken}}).
}
