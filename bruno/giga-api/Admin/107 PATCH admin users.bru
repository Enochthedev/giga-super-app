meta {
  name: PATCH admin users
  type: http
  seq: 107
}

patch {
  url: {{baseUrl}}/api/admin/users/:userId
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`PATCH /api/admin/users/:userId`

Service: **admin-service** · file: `admin-service/src/routes/users.ts` · route: `/:userId`.

Auth: admin bearer ({{adminToken}}).
}
