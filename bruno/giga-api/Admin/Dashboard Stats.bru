meta {
  name: Dashboard Stats
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/dashboard/stats
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("200", function(){ expect(res.status).to.equal(200); });
}

docs {
Admin dashboard KPIs. Requires a nipost_user_permissions row (qa.admin is DOP national).
}
