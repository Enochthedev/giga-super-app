meta {
  name: GET hotel dashboard-stats
  type: http
  seq: 64
}

get {
  url: {{baseUrl}}/api/managers/hotel/dashboard-stats
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/managers/hotel/dashboard-stats`

Service: **admin-service** · file: `admin-service/src/routes/modules/hotel.ts` · route: `/dashboard-stats`.

Auth: admin bearer ({{adminToken}}).
}
