meta {
  name: GET hotel hotels
  type: http
  seq: 60
}

get {
  url: {{baseUrl}}/api/managers/hotel/hotels/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/managers/hotel/hotels/:id`

Service: **admin-service** · file: `admin-service/src/routes/modules/hotel.ts` · route: `/hotels/:id`.

Auth: admin bearer ({{adminToken}}).
}
