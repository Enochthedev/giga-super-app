meta {
  name: Pending Entries
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/pending-entries
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

docs {
Items awaiting approval across modules (products, hotels, drivers, sellers, hosts, advertisers, media).
}
