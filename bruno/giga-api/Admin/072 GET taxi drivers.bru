meta {
  name: GET taxi drivers
  type: http
  seq: 72
}

get {
  url: {{baseUrl}}/api/managers/taxi/drivers
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/managers/taxi/drivers`

Service: **admin-service** · file: `admin-service/src/routes/modules/taxi.ts` · route: `/drivers`.

Auth: admin bearer ({{adminToken}}).
}
