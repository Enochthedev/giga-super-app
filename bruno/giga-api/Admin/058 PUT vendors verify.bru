meta {
  name: PUT vendors verify
  type: http
  seq: 58
}

put {
  url: {{baseUrl}}/api/managers/ecommerce/vendors/:id/verify
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`PUT /api/managers/ecommerce/vendors/:id/verify`

Service: **admin-service** · file: `admin-service/src/routes/modules/ecommerce-vendors.ts` · route: `/:id/verify`.

Auth: admin bearer ({{adminToken}}).
}
