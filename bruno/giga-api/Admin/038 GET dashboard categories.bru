meta {
  name: GET dashboard categories
  type: http
  seq: 38
}

get {
  url: {{baseUrl}}/api/dashboard/categories
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/dashboard/categories`

Service: **admin-service** · file: `admin-service/src/routes/dashboard.ts` · route: `/categories`.

Auth: admin bearer ({{adminToken}}).
}
