meta {
  name: POST applications approve
  type: http
  seq: 81
}

post {
  url: {{baseUrl}}/api/nipost-admin/couriers/applications/:id/approve
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "review_notes": "Approved"
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/nipost-admin/couriers/applications/:id/approve`

Service: **admin-service** · file: `admin-service/src/routes/nipost-admin.ts` · route: `/couriers/applications/:id/approve`.

Auth: admin bearer ({{adminToken}}).
}
