meta {
  name: GET taxi drivers
  type: http
  seq: 73
}

get {
  url: {{baseUrl}}/api/managers/taxi/drivers/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/managers/taxi/drivers/:id`

Service: **admin-service** · file: `admin-service/src/routes/modules/taxi.ts` · route: `/drivers/:id`.

Auth: admin bearer ({{adminToken}}).
}
