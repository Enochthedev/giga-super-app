meta {
  name: GET ecommerce revenue-chart
  type: http
  seq: 51
}

get {
  url: {{baseUrl}}/api/managers/ecommerce/revenue-chart
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/managers/ecommerce/revenue-chart`

Service: **admin-service** · file: `admin-service/src/routes/modules/ecommerce-analytics.ts` · route: `/revenue-chart`.

Auth: admin bearer ({{adminToken}}).
}
