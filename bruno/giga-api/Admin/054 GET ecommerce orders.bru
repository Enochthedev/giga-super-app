meta {
  name: GET ecommerce orders
  type: http
  seq: 54
}

get {
  url: {{baseUrl}}/api/managers/ecommerce/orders/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/managers/ecommerce/orders/:id`

Service: **admin-service** · file: `admin-service/src/routes/modules/ecommerce-orders.ts` · route: `/:id`.

Auth: admin bearer ({{adminToken}}).
}
