meta {
  name: GET ads incoming
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/ads/incoming
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/ads/incoming`

Service: **admin-service** · file: `admin-service/src/routes/advertisements.ts` · route: `/incoming`.

Auth: admin bearer ({{adminToken}}).
}
