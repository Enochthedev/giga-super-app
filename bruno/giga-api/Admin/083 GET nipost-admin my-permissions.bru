meta {
  name: GET nipost-admin my-permissions
  type: http
  seq: 83
}

get {
  url: {{baseUrl}}/api/nipost-admin/my-permissions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/nipost-admin/my-permissions`

Service: **admin-service** · file: `admin-service/src/routes/nipost-admin.ts` · route: `/my-permissions`.

Auth: admin bearer ({{adminToken}}).
}
