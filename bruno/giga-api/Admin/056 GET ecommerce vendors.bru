meta {
  name: GET ecommerce vendors
  type: http
  seq: 56
}

get {
  url: {{baseUrl}}/api/managers/ecommerce/vendors
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/managers/ecommerce/vendors`

Service: **admin-service** · file: `admin-service/src/routes/modules/ecommerce-vendors.ts` · route: `/`.

Auth: admin bearer ({{adminToken}}).
}
