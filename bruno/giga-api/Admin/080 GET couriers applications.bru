meta {
  name: GET couriers applications
  type: http
  seq: 80
}

get {
  url: {{baseUrl}}/api/nipost-admin/couriers/applications
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/nipost-admin/couriers/applications`

Service: **admin-service** · file: `admin-service/src/routes/nipost-admin.ts` · route: `/couriers/applications`.

Auth: admin bearer ({{adminToken}}).
}
