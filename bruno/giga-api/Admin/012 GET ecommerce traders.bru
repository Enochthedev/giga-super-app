meta {
  name: GET ecommerce traders
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/ecommerce/traders/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/ecommerce/traders/:id`

Service: **admin-service** · file: `admin-service/src/routes/business-modules.ts` · route: `/ecommerce/traders/:id`.

Auth: admin bearer ({{adminToken}}).
}
