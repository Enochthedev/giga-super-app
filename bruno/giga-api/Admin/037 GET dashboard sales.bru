meta {
  name: GET dashboard sales
  type: http
  seq: 37
}

get {
  url: {{baseUrl}}/api/dashboard/sales
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/dashboard/sales`

Service: **admin-service** · file: `admin-service/src/routes/dashboard.ts` · route: `/sales`.

Auth: admin bearer ({{adminToken}}).
}
