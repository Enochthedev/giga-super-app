meta {
  name: GET ecommerce traffic
  type: http
  seq: 50
}

get {
  url: {{baseUrl}}/api/managers/ecommerce/traffic
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/managers/ecommerce/traffic`

Service: **admin-service** · file: `admin-service/src/routes/modules/ecommerce-analytics.ts` · route: `/traffic`.

Auth: admin bearer ({{adminToken}}).
}
