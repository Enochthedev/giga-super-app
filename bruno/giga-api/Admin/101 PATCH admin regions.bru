meta {
  name: PATCH admin regions
  type: http
  seq: 101
}

patch {
  url: {{baseUrl}}/api/admin/regions/:id
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`PATCH /api/admin/regions/:id`

Service: **admin-service** · file: `admin-service/src/routes/regions.ts` · route: `/:id`.

Auth: admin bearer ({{adminToken}}).
}
