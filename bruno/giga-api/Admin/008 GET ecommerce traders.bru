meta {
  name: GET ecommerce traders
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/api/ecommerce/traders
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/ecommerce/traders`

Service: **admin-service** · file: `admin-service/src/routes/business-modules.ts` · route: `/ecommerce/traders`.

Auth: admin bearer ({{adminToken}}).
}
