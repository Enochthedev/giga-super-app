meta {
  name: DELETE media files
  type: http
  seq: 67
}

delete {
  url: {{baseUrl}}/api/managers/media/files/:id
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`DELETE /api/managers/media/files/:id`

Service: **admin-service** · file: `admin-service/src/routes/modules/media.ts` · route: `/files/:id`.

Auth: admin bearer ({{adminToken}}).
}
