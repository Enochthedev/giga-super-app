meta {
  name: Users
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/admin/users
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

params:query {
  limit: 20
}

docs {
List platform users (region-scoped for regional admins).
}
