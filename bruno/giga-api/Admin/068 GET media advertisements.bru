meta {
  name: GET media advertisements
  type: http
  seq: 68
}

get {
  url: {{baseUrl}}/api/managers/media/advertisements
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/managers/media/advertisements`

Service: **admin-service** · file: `admin-service/src/routes/modules/media.ts` · route: `/advertisements`.

Auth: admin bearer ({{adminToken}}).
}
