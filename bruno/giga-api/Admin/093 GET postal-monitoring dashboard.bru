meta {
  name: GET postal-monitoring dashboard
  type: http
  seq: 93
}

get {
  url: {{baseUrl}}/api/postal-monitoring/dashboard
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/postal-monitoring/dashboard`

Service: **admin-service** · file: `admin-service/src/routes/postal-monitoring.ts` · route: `/dashboard`.

Auth: admin bearer ({{adminToken}}).
}
