meta {
  name: POST pending-entries reject
  type: http
  seq: 20
}

post {
  url: {{baseUrl}}/api/pending-entries/:module/:id/reject
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "reason": "Does not meet criteria"
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/pending-entries/:module/:id/reject`

Service: **admin-service** · file: `admin-service/src/routes/business-modules.ts` · route: `/pending-entries/:module/:id/reject`.

Auth: admin bearer ({{adminToken}}).
}
