meta {
  name: POST staff approve
  type: http
  seq: 25
}

post {
  url: {{baseUrl}}/api/postal/staff/:id/approve
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "review_notes": "Approved"
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/postal/staff/:id/approve`

Service: **admin-service** · file: `admin-service/src/routes/business-modules.ts` · route: `/postal/staff/:id/approve`.

Auth: admin bearer ({{adminToken}}).
}
