meta {
  name: GET postal-staff applications
  type: http
  seq: 77
}

get {
  url: {{baseUrl}}/api/nipost-admin/postal-staff/applications
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/nipost-admin/postal-staff/applications`

Service: **admin-service** · file: `admin-service/src/routes/nipost-admin.ts` · route: `/postal-staff/applications`.

Auth: admin bearer ({{adminToken}}).
}
