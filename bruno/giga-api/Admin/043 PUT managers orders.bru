meta {
  name: PUT managers orders
  type: http
  seq: 43
}

put {
  url: {{baseUrl}}/api/managers/orders/:orderId
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`PUT /api/managers/orders/:orderId`

Service: **admin-service** · file: `admin-service/src/routes/managers.ts` · route: `/orders/:orderId`.

Auth: admin bearer ({{adminToken}}).
}
