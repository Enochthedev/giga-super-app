meta {
  name: GET state financial-summary
  type: http
  seq: 89
}

get {
  url: {{baseUrl}}/api/admin/state/:stateId/financial-summary
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/admin/state/:stateId/financial-summary`

Service: **admin-service** · file: `admin-service/src/routes/nipost.ts` · route: `/state/:stateId/financial-summary`.

Auth: admin bearer ({{adminToken}}).
}
