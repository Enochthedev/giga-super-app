meta {
  name: POST admin users
  type: http
  seq: 105
}

post {
  url: {{baseUrl}}/api/admin/users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/admin/users`

Service: **admin-service** · file: `admin-service/src/routes/users.ts` · route: `/`.

Auth: admin bearer ({{adminToken}}).
}
