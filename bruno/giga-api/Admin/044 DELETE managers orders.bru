meta {
  name: DELETE managers orders
  type: http
  seq: 44
}

delete {
  url: {{baseUrl}}/api/managers/orders/:orderId
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`DELETE /api/managers/orders/:orderId`

Service: **admin-service** · file: `admin-service/src/routes/managers.ts` · route: `/orders/:orderId`.

Auth: admin bearer ({{adminToken}}).
}
