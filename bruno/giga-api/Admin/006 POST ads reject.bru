meta {
  name: POST ads reject
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/ads/:adId/reject
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "reason": "Does not meet criteria"
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/ads/:adId/reject`

Service: **admin-service** · file: `admin-service/src/routes/advertisements.ts` · route: `/:adId/reject`.

Auth: admin bearer ({{adminToken}}).
}
