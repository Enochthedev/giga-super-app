meta {
  name: GET media files
  type: http
  seq: 65
}

get {
  url: {{baseUrl}}/api/managers/media/files
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/managers/media/files`

Service: **admin-service** · file: `admin-service/src/routes/modules/media.ts` · route: `/files`.

Auth: admin bearer ({{adminToken}}).
}
