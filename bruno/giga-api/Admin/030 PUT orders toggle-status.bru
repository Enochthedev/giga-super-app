meta {
  name: PUT orders toggle-status
  type: http
  seq: 30
}

put {
  url: {{baseUrl}}/api/delivery/orders/:orderId/toggle-status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "status": "active"
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`PUT /api/delivery/orders/:orderId/toggle-status`

Service: **admin-service** · file: `admin-service/src/routes/business-modules.ts` · route: `/delivery/orders/:orderId/toggle-status`.

Auth: admin bearer ({{adminToken}}).
}
