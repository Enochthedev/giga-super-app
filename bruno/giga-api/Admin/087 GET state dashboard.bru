meta {
  name: GET state dashboard
  type: http
  seq: 87
}

get {
  url: {{baseUrl}}/api/admin/state/:stateId/dashboard
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/admin/state/:stateId/dashboard`

Service: **admin-service** · file: `admin-service/src/routes/nipost.ts` · route: `/state/:stateId/dashboard`.

Auth: admin bearer ({{adminToken}}).
}
