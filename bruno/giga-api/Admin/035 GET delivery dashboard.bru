meta {
  name: GET delivery dashboard
  type: http
  seq: 35
}

get {
  url: {{baseUrl}}/api/delivery/dashboard
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/delivery/dashboard`

Service: **admin-service** · file: `admin-service/src/routes/business-modules.ts` · route: `/delivery/dashboard`.

Auth: admin bearer ({{adminToken}}).
}
