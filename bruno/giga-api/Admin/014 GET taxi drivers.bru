meta {
  name: GET taxi drivers
  type: http
  seq: 14
}

get {
  url: {{baseUrl}}/api/taxi/drivers/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/taxi/drivers/:id`

Service: **admin-service** · file: `admin-service/src/routes/business-modules.ts` · route: `/taxi/drivers/:id`.

Auth: admin bearer ({{adminToken}}).
}
