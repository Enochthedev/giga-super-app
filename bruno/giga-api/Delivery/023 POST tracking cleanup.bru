meta {
  name: POST tracking cleanup
  type: http
  seq: 23
}

post {
  url: {{baseUrl}}/api/v1/tracking/:assignmentId/cleanup
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/tracking/:assignmentId/cleanup`

Service: **delivery-service** · file: `delivery-service/src/routes/tracking.ts` · route: `/tracking/:assignmentId/cleanup`.

Auth: bearer ({{token}}).
}
