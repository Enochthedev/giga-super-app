meta {
  name: GET tracking progress
  type: http
  seq: 22
}

get {
  url: {{baseUrl}}/api/v1/tracking/:assignmentId/progress
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/tracking/:assignmentId/progress`

Service: **delivery-service** · file: `delivery-service/src/routes/tracking.ts` · route: `/tracking/:assignmentId/progress`.

Auth: bearer ({{token}}).
}
