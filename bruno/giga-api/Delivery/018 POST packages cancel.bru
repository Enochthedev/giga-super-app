meta {
  name: POST packages cancel
  type: http
  seq: 18
}

post {
  url: {{baseUrl}}/api/v1/packages/:packageId/cancel
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "reason": "changed plans"
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/packages/:packageId/cancel`

Service: **delivery-service** · file: `delivery-service/src/routes/packages.ts` · route: `/:packageId/cancel`.

Auth: bearer ({{token}}).
}
