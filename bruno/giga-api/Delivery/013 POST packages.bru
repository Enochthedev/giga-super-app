meta {
  name: POST packages
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/api/v1/packages
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/packages`

Service: **delivery-service** · file: `delivery-service/src/routes/packages.ts` · route: `/`.

Auth: bearer ({{token}}).
}
