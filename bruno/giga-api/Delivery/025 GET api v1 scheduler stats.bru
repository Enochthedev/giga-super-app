meta {
  name: GET api v1 scheduler stats
  type: http
  seq: 25
}

get {
  url: {{baseUrl}}/api/v1/scheduler/stats
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("reached backend (verified 403 on 2026-08-18)", function(){
    expect(res.status).to.be.below(500);
  });
}

docs {
  `GET /api/v1/scheduler/stats`
  
  **🔒 403 for the QA account** — last verified 2026-08-18, HTTP `403`.
  
  Service: **delivery-service** · file: `delivery-service/src/routes/scheduler.ts`
  Auth: customer bearer (`{{token}}`).
}
