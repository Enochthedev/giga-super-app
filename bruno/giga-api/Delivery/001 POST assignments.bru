meta {
  name: POST assignments
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/assignments
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/assignments`

Service: **delivery-service** · file: `delivery-service/src/routes/assignments.ts` · route: `/`.

Auth: bearer ({{token}}).
}
