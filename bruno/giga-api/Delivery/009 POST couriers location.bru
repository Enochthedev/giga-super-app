meta {
  name: POST couriers location
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/v1/couriers/:courierId/location
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "latitude": 6.5244,
    "longitude": 3.3792
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/couriers/:courierId/location`

Service: **delivery-service** · file: `delivery-service/src/routes/couriers.ts` · route: `/couriers/:courierId/location`.

Auth: bearer ({{token}}).
}
