meta {
  name: POST track-delivery
  type: http
  seq: 20
}

post {
  url: {{baseUrl}}/api/v1/track-delivery
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/track-delivery`

Service: **delivery-service** · file: `delivery-service/src/routes/tracking.ts` · route: `/track-delivery`.

Auth: bearer ({{token}}).
}
