meta {
  name: GET api v1 websocket stats
  type: http
  seq: 28
}

get {
  url: {{baseUrl}}/api/v1/websocket/stats
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("reached backend (verified 403 on 2026-08-18)", function(){
    expect(res.status).to.be.below(500);
  });
}

docs {
  `GET /api/v1/websocket/stats`
  
  **🔒 403 for the QA account** — last verified 2026-08-18, HTTP `403`.
  
  Service: **delivery-service** · file: `delivery-service/src/routes/websocket.ts`
  Auth: customer bearer (`{{token}}`).
}
