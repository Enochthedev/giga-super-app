meta {
  name: GET packages sender
  type: http
  seq: 15
}

get {
  url: {{baseUrl}}/api/v1/packages/sender/:senderId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/packages/sender/:senderId`

Service: **delivery-service** · file: `delivery-service/src/routes/packages.ts` · route: `/sender/:senderId`.

Auth: bearer ({{token}}).
}
