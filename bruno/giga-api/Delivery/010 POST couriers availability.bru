meta {
  name: POST couriers availability
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/api/v1/couriers/:courierId/availability
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/couriers/:courierId/availability`

Service: **delivery-service** · file: `delivery-service/src/routes/couriers.ts` · route: `/couriers/:courierId/availability`.

Auth: bearer ({{token}}).
}
