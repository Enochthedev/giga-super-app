meta {
  name: GET couriers
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/v1/couriers/:courierId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/couriers/:courierId`

Service: **delivery-service** · file: `delivery-service/src/routes/couriers.ts` · route: `/couriers/:courierId`.

Auth: bearer ({{token}}).
}
