meta {
  name: POST couriers
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/couriers
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/couriers`

Service: **delivery-service** · file: `delivery-service/src/routes/couriers.ts` · route: `/couriers`.

Auth: bearer ({{token}}).
}
