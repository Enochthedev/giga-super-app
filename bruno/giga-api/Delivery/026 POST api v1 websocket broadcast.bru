meta {
  name: POST api v1 websocket broadcast
  type: http
  seq: 26
}

post {
  url: {{baseUrl}}/api/v1/websocket/broadcast
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "_replace": "real fields required - never send an empty {} body (defect V3)"
  }
}

tests {
  // KNOWN DEFECT: verified 404 on 2026-08-18. Flip to a 2xx assertion once fixed.
  test("known defect still reproduces (404)", function(){ expect(res.status).to.equal(404); });
}

docs {
  `POST /api/v1/websocket/broadcast`
  
  **🔴 BROKEN — no gateway route (defect V5)** — last verified 2026-08-18, HTTP `404`.
  
  Service: **delivery-service** · file: `delivery-service/src/routes/websocket.ts`
  Auth: customer bearer (`{{token}}`).
  
  > ⚠️ Known defect — see `docs/API_VERIFICATION_2026-08-18.md`. Observed: `404` No service available for this endpoint
}
