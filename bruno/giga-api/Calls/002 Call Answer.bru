meta {
  name: Call Answer
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/calls/answer
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "conversationId": "REPLACE_CONV",
    "callType": "audio",
    "participantIds": [
      "{{customer2Id}}"
    ]
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`answer-call` edge fn.
}
