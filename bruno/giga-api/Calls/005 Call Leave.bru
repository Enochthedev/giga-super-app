meta {
  name: Call Leave
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/calls/leave
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "conversationId": "REPLACE_CONV",
    "callType": "audio",
    "participantIds": [
      "{{customer2Id}}"
    ]
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`leave-call` edge fn.
}
