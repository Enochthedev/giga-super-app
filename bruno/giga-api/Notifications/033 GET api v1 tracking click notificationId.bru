meta {
  name: GET api v1 tracking click notificationId
  type: http
  seq: 33
}

get {
  url: {{baseUrl}}/api/v1/tracking/click/:notificationId
  body: none
  auth: bearer
}

params:path {
  notificationId: 00000000-0000-0000-0000-000000000000
}

auth:bearer {
  token: {{token}}
}

tests {
  test("reached backend (verified 400 on 2026-08-18)", function(){
    expect(res.status).to.be.below(500);
  });
}

docs {
  `GET /api/v1/tracking/click/:notificationId`
  
  **✅ reachable (probe payload → 400)** — last verified 2026-08-18, HTTP `400`.
  
  Service: **notifications-service** · file: `notifications-service/src/routes/tracking.ts`
  Auth: customer bearer (`{{token}}`).
}
