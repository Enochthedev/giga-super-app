meta {
  name: GET notifications status
  type: http
  seq: 16
}

get {
  url: {{baseUrl}}/api/v1/notifications/status/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/notifications/status/:id`

Service: **notifications-service** · file: `notifications-service/src/routes/notifications.ts` · route: `/status/:id`.

Auth: bearer ({{token}}).
}
