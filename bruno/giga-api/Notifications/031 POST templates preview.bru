meta {
  name: POST templates preview
  type: http
  seq: 31
}

post {
  url: {{baseUrl}}/api/v1/templates/:id/preview
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/templates/:id/preview`

Service: **notifications-service** · file: `notifications-service/src/routes/templates.ts` · route: `/:id/preview`.

Auth: bearer ({{token}}).
}
