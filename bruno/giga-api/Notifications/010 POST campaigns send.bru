meta {
  name: POST campaigns send
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/api/v1/campaigns/:id/send
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/campaigns/:id/send`

Service: **notifications-service** · file: `notifications-service/src/routes/campaigns.ts` · route: `/:id/send`.

Auth: bearer ({{token}}).
}
