meta {
  name: POST campaigns cancel
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/api/v1/campaigns/:id/cancel
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "reason": "changed plans"
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/campaigns/:id/cancel`

Service: **notifications-service** · file: `notifications-service/src/routes/campaigns.ts` · route: `/:id/cancel`.

Auth: bearer ({{token}}).
}
