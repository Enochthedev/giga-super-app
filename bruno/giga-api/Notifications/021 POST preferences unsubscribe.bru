meta {
  name: POST preferences unsubscribe
  type: http
  seq: 21
}

post {
  url: {{baseUrl}}/api/v1/preferences/unsubscribe
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/preferences/unsubscribe`

Service: **notifications-service** · file: `notifications-service/src/routes/preferences.ts` · route: `/unsubscribe`.

Auth: bearer ({{token}}).
}
