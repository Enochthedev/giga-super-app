meta {
  name: Preferences
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/preferences
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
Get notification preferences. (Real path is /api/v1/preferences, not /notifications/preferences.)
}
