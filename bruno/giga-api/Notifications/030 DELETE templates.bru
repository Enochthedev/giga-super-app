meta {
  name: DELETE templates
  type: http
  seq: 30
}

delete {
  url: {{baseUrl}}/api/v1/templates/:id
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`DELETE /api/v1/templates/:id`

Service: **notifications-service** · file: `notifications-service/src/routes/templates.ts` · route: `/:id`.

Auth: bearer ({{token}}).
}
