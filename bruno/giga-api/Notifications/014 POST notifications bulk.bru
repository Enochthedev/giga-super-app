meta {
  name: POST notifications bulk
  type: http
  seq: 14
}

post {
  url: {{baseUrl}}/api/v1/notifications/bulk
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/notifications/bulk`

Service: **notifications-service** · file: `notifications-service/src/routes/notifications.ts` · route: `/bulk`.

Auth: bearer ({{token}}).
}
