meta {
  name: POST templates validate
  type: http
  seq: 32
}

post {
  url: {{baseUrl}}/api/v1/templates/validate
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/templates/validate`

Service: **notifications-service** · file: `notifications-service/src/routes/templates.ts` · route: `/validate`.

Auth: bearer ({{token}}).
}
