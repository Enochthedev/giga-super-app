meta {
  name: GET analytics volume
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/analytics/volume
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/analytics/volume`

Service: **notifications-service** · file: `notifications-service/src/routes/analytics.ts` · route: `/volume`.

Auth: bearer ({{token}}).
}
