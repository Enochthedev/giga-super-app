meta {
  name: GET analytics templates
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/analytics/templates
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/analytics/templates`

Service: **notifications-service** · file: `notifications-service/src/routes/analytics.ts` · route: `/templates`.

Auth: bearer ({{token}}).
}
