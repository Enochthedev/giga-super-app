meta {
  name: GET campaigns stats
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/api/v1/campaigns/:id/stats
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/campaigns/:id/stats`

Service: **notifications-service** · file: `notifications-service/src/routes/campaigns.ts` · route: `/:id/stats`.

Auth: bearer ({{token}}).
}
