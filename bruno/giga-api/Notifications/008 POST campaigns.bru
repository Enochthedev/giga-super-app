meta {
  name: POST campaigns
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/campaigns
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/campaigns`

Service: **notifications-service** · file: `notifications-service/src/routes/campaigns.ts` · route: `/`.

Auth: bearer ({{token}}).
}
