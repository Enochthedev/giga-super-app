meta {
  name: GET analytics users
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/analytics/users
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/analytics/users`

Service: **notifications-service** · file: `notifications-service/src/routes/analytics.ts` · route: `/users`.

Auth: bearer ({{token}}).
}
