meta {
  name: GET preferences unsubscribe
  type: http
  seq: 22
}

get {
  url: {{baseUrl}}/api/v1/preferences/unsubscribe/:token
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/preferences/unsubscribe/:token`

Service: **notifications-service** · file: `notifications-service/src/routes/preferences.ts` · route: `/unsubscribe/:token`.

Auth: bearer ({{token}}).
}
