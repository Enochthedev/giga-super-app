meta {
  name: PUT campaigns
  type: http
  seq: 9
}

put {
  url: {{baseUrl}}/api/v1/campaigns/:id
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`PUT /api/v1/campaigns/:id`

Service: **notifications-service** · file: `notifications-service/src/routes/campaigns.ts` · route: `/:id`.

Auth: bearer ({{token}}).
}
