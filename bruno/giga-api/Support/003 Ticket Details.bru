meta {
  name: Ticket Details
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/support/tickets/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`get-ticket-details`. Replace :id.
}
