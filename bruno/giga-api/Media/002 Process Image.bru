meta {
  name: Process Image
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/media/process-image
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "fileId": "REPLACE_FILE_ID"
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`process-image` — generates thumbnails.
}
