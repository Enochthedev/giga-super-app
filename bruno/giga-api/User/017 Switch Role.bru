meta {
  name: Switch Role
  type: http
  seq: 17
}

post {
  url: {{baseUrl}}/api/v1/user/switch-role
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "role": "customer"
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`switch-role` edge fn.
}
