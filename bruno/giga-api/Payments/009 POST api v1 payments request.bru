meta {
  name: POST api v1 payments request
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/api/v1/payments/request
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "_replace": "real fields required - never send an empty {} body (defect V3)"
  }
}

tests {
  // KNOWN DEFECT: verified 503 on 2026-08-18. Flip to a 2xx assertion once fixed.
  test("known defect still reproduces (503)", function(){ expect(res.status).to.equal(503); });
}

docs {
  `POST /api/v1/payments/request`
  
  **🔴 BROKEN — backend 503 (defect V1)** — last verified 2026-08-18, HTTP `503`.
  
  Service: **payment-queue-service** · file: `payment-queue-service/src/routes/v1/payments.ts`
  Auth: customer bearer (`{{token}}`).
  
  > ⚠️ Known defect — see `docs/API_VERIFICATION_2026-08-18.md`. Observed: `503` Payment Queue Service is currently unavailable
}
