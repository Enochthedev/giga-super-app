meta {
  name: Create Payment Intent
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/payments/intent
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "amount": 5000,
    "currency": "NGN"
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`create-payment-intent` edge fn.
}
