meta {
  name: Verify Payment
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/payments/verify
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "transactionId": "REPLACE_TXN"
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`Verify-payment` edge fn.
}
