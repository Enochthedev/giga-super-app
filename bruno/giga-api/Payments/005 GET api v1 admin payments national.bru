meta {
  name: GET api v1 admin payments national
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/admin/payments/national
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  // KNOWN DEFECT: verified 404 on 2026-08-18. Flip to a 2xx assertion once fixed.
  test("known defect still reproduces (404)", function(){ expect(res.status).to.equal(404); });
}

docs {
  `GET /api/v1/admin/payments/national`
  
  **🔴 BROKEN — routed to wrong service (defect V6)** — last verified 2026-08-18, HTTP `404`.
  
  Service: **payment-queue-service** · file: `payment-queue-service/src/routes/v1/admin.ts`
  Auth: customer bearer (`{{token}}`).
  
  > ⚠️ Known defect — see `docs/API_VERIFICATION_2026-08-18.md`. Observed: `404` Endpoint not found
}
