meta {
  name: GET api v1 payments paymentId status
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/api/v1/payments/:paymentId/status
  body: none
  auth: bearer
}

params:path {
  paymentId: 00000000-0000-0000-0000-000000000000
}

auth:bearer {
  token: {{token}}
}

tests {
  test("reached backend (verified 404 on 2026-08-18)", function(){
    expect(res.status).to.be.below(500);
  });
}

docs {
  `GET /api/v1/payments/:paymentId/status`
  
  **✅ reachable (ghost id → 404)** — last verified 2026-08-18, HTTP `404`.
  
  Service: **payment-queue-service** · file: `payment-queue-service/src/routes/v1/payments.ts`
  Auth: customer bearer (`{{token}}`).
}
