meta {
  name: Wallet Balance
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/wallet/balance
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
Wallet balance. KNOWN BUG (E1): wallet edge fns currently reject valid tokens ('Authentication required').
}
