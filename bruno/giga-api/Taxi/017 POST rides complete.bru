meta {
  name: POST rides complete
  type: http
  seq: 17
}

post {
  url: {{baseUrl}}/api/v1/rides/:rideId/complete
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/rides/:rideId/complete`

Service: **taxi-realtime-service** · file: `taxi-realtime-service/src/routes/rides.ts` · route: `/:rideId/complete`.

Auth: bearer ({{token}}).
}
