meta {
  name: PUT drivers location
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/api/v1/drivers/location
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "latitude": 6.5244,
    "longitude": 3.3792
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`PUT /api/v1/drivers/location`

Service: **taxi-realtime-service** · file: `taxi-realtime-service/src/routes/drivers.ts` · route: `/location`.

Auth: bearer ({{token}}).
}
