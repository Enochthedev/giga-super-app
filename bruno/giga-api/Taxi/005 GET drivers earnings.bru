meta {
  name: GET drivers earnings
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/drivers/earnings
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/drivers/earnings`

Service: **taxi-realtime-service** · file: `taxi-realtime-service/src/routes/drivers.ts` · route: `/earnings`.

Auth: bearer ({{token}}).
}
