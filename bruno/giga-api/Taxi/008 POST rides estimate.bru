meta {
  name: POST rides estimate
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/rides/estimate
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "pickup_lat": 6.5244,
    "pickup_lng": 3.3792,
    "dropoff_lat": 6.4281,
    "dropoff_lng": 3.4219,
    "vehicle_type": "standard"
  }
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/rides/estimate`

Service: **taxi-realtime-service** · file: `taxi-realtime-service/src/routes/rides.ts` · route: `/estimate`.

Auth: bearer ({{token}}).
}
