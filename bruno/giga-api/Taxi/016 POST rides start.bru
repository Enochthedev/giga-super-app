meta {
  name: POST rides start
  type: http
  seq: 16
}

post {
  url: {{baseUrl}}/api/v1/rides/:rideId/start
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/rides/:rideId/start`

Service: **taxi-realtime-service** · file: `taxi-realtime-service/src/routes/rides.ts` · route: `/:rideId/start`.

Auth: bearer ({{token}}).
}
