meta {
  name: POST rides rate
  type: http
  seq: 20
}

post {
  url: {{baseUrl}}/api/v1/rides/:rideId/rate
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/rides/:rideId/rate`

Service: **taxi-realtime-service** · file: `taxi-realtime-service/src/routes/rides.ts` · route: `/:rideId/rate`.

Auth: bearer ({{token}}).
}
