meta {
  name: POST rides accept
  type: http
  seq: 15
}

post {
  url: {{baseUrl}}/api/v1/rides/:rideId/accept
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`POST /api/v1/rides/:rideId/accept`

Service: **taxi-realtime-service** · file: `taxi-realtime-service/src/routes/rides.ts` · route: `/:rideId/accept`.

Auth: bearer ({{token}}).
}
