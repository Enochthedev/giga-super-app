meta {
  name: GET drivers nearby
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/drivers/nearby
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds", function(){ expect(res.status).to.be.below(500); });
}

docs {
`GET /api/v1/drivers/nearby`

Service: **taxi-realtime-service** · file: `taxi-realtime-service/src/routes/drivers.ts` · route: `/nearby`.

Auth: bearer ({{token}}).
}
