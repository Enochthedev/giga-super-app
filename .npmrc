# TypeScript's declaration emit cannot name types that live under pnpm's nested
# .pnpm paths (TS2742 on every Express Router), so `pnpm run build` fails. Hoist
# the type packages where tsc can name them.
public-hoist-pattern[]=@types/*
